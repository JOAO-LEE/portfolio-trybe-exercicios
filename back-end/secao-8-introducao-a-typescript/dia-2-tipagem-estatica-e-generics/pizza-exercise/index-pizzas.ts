import {IPizza} from './interface';

const pepperoni: IPizza = {
  flavor: 'Calabresa',
  slices: 8,
};

const marguerita: IPizza = {
  flavor: 'Marguerita',
  slices: 6,
};

const nutella: IPizza = {
  flavor: 'Nutella',
  slices: 4,
}